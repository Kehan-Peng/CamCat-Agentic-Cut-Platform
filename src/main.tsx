import React from "react";
import ReactDOM from "react-dom/client";
import NovaWorkspacePage from "../NovaWorkspacePage";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <NovaWorkspacePage />
  </React.StrictMode>,
);
